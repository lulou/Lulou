- [Single-session enforcement](single-session-enforcement.md) — full architecture: session-verify on INITIAL_SESSION, X-Session-Id middleware gate, realtime broadcast on replace, heartbeat conditional UPDATE. Fail-open policy on 5xx.
- [Safari keyframe crash pattern](safari-keyframe-crash.md) — inline animation styles that reference @keyframes injected via useEffect crash Safari/WebKit; inject at module scope instead.
- [i18n variable shadowing](i18n-variable-shadowing.md) — adding `const { t } = useLanguageContext()` to files that use `const t = setTimeout/requestAnimationFrame` inside useEffect causes TS shadowing errors; rename local vars.
- [Units context pattern](units-context.md) — useUnits is a global context (UnitsProvider in App.tsx), so unit changes propagate app-wide instantly. units.ts re-exports useUnitsContext as useUnits for backward compat.
- [Settings i18n + legal pages](settings-i18n.md) — All settings section headers and row labels use t() keys. Legal pages (Privacy, Terms, Download Data, Safety Tips, Member Principles, Licences, Privacy Preferences) are in-app bottom sheets, not external links. Data export via GET /api/profile/export.
- [Call rering stale guard](call-rering-stale.md) — rerMatch useMemo must include isArmedSession() + endedMatchIdsRef + 90s age check + armedTick/endedTick deps to prevent endless rererings from stale DB rows after a call ends.
- [i18n new-key fallback](i18n-new-key-fallback.md) — getTranslation falls back to English for any missing key in non-English languages, so new keys only need to be added to the "en" block; other languages get English text automatically.
- [Profile column guard flag pattern](profile-column-guards.md) — MATCH_PROFILE_COLS must be a function (getMatchProfileCols) not a const; module-level const spreads evaluate before guard flags are set. POOL_COLS and WHEEL_COLS are safe inside functions. (profile as any).field ?? [] loses type; cast the result explicitly.
- [Embla carousel photo animation](embla-photo-overlay.md) — Re-keying an inner wrapper div doesn't work because Embla's `select` fires AFTER the slide transition; correct pattern is an absolutely-positioned overlay div that plays the CSS animation simultaneously with an instant `scrollTo(idx, true)` call.
- [Stacked cards carousel pattern](stacked-cards-carousel.md) — flex-strip + overflow:hidden clips all box-shadows; use stacked position:absolute cards with container padding:SHADOW_PAD so shadow bleeds into padding zone and IS visible. Both PhotoCarousel and ProfilePhotoViewer use this pattern.
- [forcedIncomingMatch missing guards](forced-incoming-call-bug.md) — forcedIncomingMatch in App.tsx must check dismissedCallKey and !!m.callInitiatorId; without these, pressing Decline sets dismissedCallKey but forcedIncomingMatch bypasses it → overlay re-mounts immediately as random incoming call.
- [Stripe activate idempotency](stripe-activate-idempotency.md) — extras-activate and elevate-activate had no dedup; fixed with processed_stripe_sessions table (PK=sessionId); catch code 23505 to return early success without re-granting.
- [Phantom call / missed ring bugs](phantom-call-bugs.md) — three root causes fixed in call signaling; see topic file.
- [Call polling gate stale reset](call-polling-gate-reset.md) — hasRingRef.current must be reset when all call memos return null; stale-call timeout (90 s) bypasses the normal callEndedCallback path and leaves it permanently true, freezing refetchInterval.
- [Voice notes cross-platform](voice-notes-cross-platform.md) — Safari/iOS uses audio/mp4 (fragmented MP4); needs -fflags +genpts+igndts in FFmpeg. Production env requires ffmpeg-static package; system ffmpeg only exists in dev (NixOS). ENOENT = missing binary, not bad audio.
- [iOS voice notes root causes](voice-notes-ios-bugs.md) — Five root causes: MIME type empty string on iOS, visualViewport not gated during recording, silent blob.size=0 onstop, CDN propagation <10s, recorder.mimeType must be preferred over requested mimeType.
- [Email verification system](email-verification-system.md) — Full 3-layer email verification system; detectSessionInUrl MUST be true; PASSWORD_RECOVERY event handled via passwordRecovery state in AuthContext; admin diagnostics at /admin/diagnostics gated by ADMIN_EMAIL env var.
- [Auth callback page pattern](auth-callback-pattern.md) — All Supabase email links must point to /auth/callback; INITIAL_SESSION(session) must be handled alongside SIGNED_IN; use-auth.ts must call setIsLoading(true) at SIGNED_IN start so / shows spinner not Landing while session-check IIFE runs.
- [Vercel blank page crash chain](vercel-blank-page-crash.md) — supabase.ts SUPABASE_KEY_LEN=key.length throws TypeError if VITE_SUPABASE_ANON_KEY missing; createClient(undefined,undefined) throws; crash propagates to queryClient→App→React never mounts→blank. Fixed with safe fallbacks+supabaseConfigError+AppRootErrorBoundary+index.html pre-module error handler.
- [checkEmailVerified admin API timeout](check-email-verified-timeout.md) — supabaseAdmin.auth.admin.getUserById() hangs indefinitely in Replit (same UND_ERR_HEADERS_TIMEOUT as supabase.auth.getUser()); must be wrapped in Promise.race with 2500ms timeout or every cache-miss /api/* request blocks → reconnect screen.
- [stripe-replit-sync removal](stripe-replit-sync-removal.md) — .replit integration entries can't be edited directly; to unblock "Finish Stripe sandbox setup" publish gate, replace stripe-replit-sync with direct stripe.webhooks.constructEvent(); webhook secret fallback reads stripe._managed_webhooks table.
- [blocked_contacts is phone-only](blocked-contacts-schema.md) — local blocked_contacts table stores phone/email contact blocks (no blockedUserId). No local user-to-user block table exists; isBlockedBy() in pushService is a documented stub returning false until one is added.
- [speakerOn default must be false](speakeron-false-required.md) — speakerOn=isVideo causes iPhone screeching; configureVoiceChat/setSpeaker are Capacitor-only no-ops on plain Safari.
- [iOS PWA push safeOptions](ios-push-safeoptions.md) — Tier-1 showNotification must use ONLY {body, tag}; any extra property (data, icon, badge) silently causes iOS 16.4-17.x to show "AppName — Notification" fallback. Reconstruct URL from tag in notificationclick.
- [Call event type naming](call-event-types.md) — Server stores: cancelled (caller hung up while ringing), declined (callee pressed decline), ended (normal end). Display is perspective-aware: cancelled+isMe→"You called {name}", cancelled+!isMe→"Missed call from {name}".
- [Session boot race bugs](session-boot-race-bugs.md) — TOKEN_REFRESHED can bypass asyncIIFE loading gate; PASSWORD_RECOVERY needs session-check; middleware must return invalid_session (no row) vs session_replaced (wrong session) for correct client recovery.
- [False iPhone auto-logout race](false-iphone-logout-race.md) — same-device bootstrap poisoned cache with "session_replaced"; stale in-flight React Query / heartbeat requests then got forced-logout. Fix: cache stores reason; client stale-request guard compares sent vs current sessionId.
- [Discover session race](discover-session-race.md) — bfcache prefetch fires with stale session ID → invalid_session → Discover error. Fix: isSessionReady gate on prefetch, stale invalid_session guard in getQueryFn, reset errored queries after bootstrap, debug panel on error screen.
- [Stale sessionBootstrapFailed race](stale-bootstrap-failed-race.md) — SIGNED_IN fails first → sets failure flag; INITIAL_SESSION succeeds but never cleared it → retry screen with valid session. Fix: generation counter (authAttemptRef) + setSessionBootstrapFailed(false) on ALL success paths + isSessionReadyRef guard on session-bootstrap-needed event.
- [Session verify retry flow](session-verify-retry-flow.md) — verify-first retry strategy; useLayoutEffect for isSessionReadyRef sync; full bootstrap diagnostic keys (bootstrap_http, bootstrap_body, failure_branch, retry_*); App.tsx panel uses DiagPanelInner.
- [Auth fetch timeouts](auth-fetch-timeouts.md) — session-verify and session-bootstrap fetches had no timeout; on iOS PWA cold open the IIFE never resolved → authLoading=true forever. Fix: 8s AbortController on each verify fetch + 10s on bootstrap. App.tsx adds 8s hard timeout → "Reload App" screen with full diagnostic panel.
- [Settings persistence](settings-persistence.md) — server is authoritative; GET creates defaults on first call; query keyed by userId; optimistic mutation with rollback; no unscoped localStorage; push toggle = accountPreference only.
- [Call gate progression rendering bugs](call-gate-progression-bugs.md) — bothStageComplete required (not partnerAtLimit or firstCallPromptSeen); waitingForPartner card shown when only one user done; lastKnownStageRef reset on matchId change; server rejects unless both counts >= 15.
- [Call availability data format](call-availability-format.md) — Presets converted to absolute UTC timestamps at client click time; server computes agreed_call_at (10-min tolerance); 5-min early-start window; new columns call_avail_1_at/call_avail_2_at/agreed_call_at; CALL_SCHEDULED state added.
- [Availability expiry + race fix](availability-expiry-race.md) — 30-min grace window after agreed_call_at (server+client); two-step write in setCallAvailability; AVAILABILITY_EXPIRED state; nowMs 30s ticker for live countdown.
- [Message counter split-DB fix](message-counter-split-db.md) — counter was written to Railway (Drizzle db), reads go to Supabase; fixed by RPC in Supabase. Fallback removed — RPC failure now 503. Stage 1 also increments now. Client refetches detail after patch.
- [Supabase matches.id is TEXT](supabase-matches-id-type.md) — matches.id is TEXT not UUID; RPC params must use TEXT or "operator does not exist: text = uuid" at runtime.
- [WAL refetch race causes badge snap-back](wal-refetch-race.md) — message insert WAL → useUnreadCounts invalidates match-detail → refetch races counter update → reads 0 → snaps badge to 15. Fix: increment counter BEFORE inserting message.
- [Replit blocked from Supabase PostgreSQL](replit-supabase-connectivity.md) — Management API 401 (sbp_ PAT unauthorized), direct TCP DNS-blocked (even with DoH). Only HTTP to PostgREST works. DDL must be applied manually via Supabase SQL editor.
