PROFILE PAGE — FIX LULOU HEADER SCROLLING

Look at the attached screenshot.

CURRENT BUG

On the Profile page, the top Lulou header containing:

- Lulou logo
- “Lulou” wordmark
- logout icon

stays fixed at the top of the screen while the Profile content scrolls underneath it.

You can clearly see the profile name/content disappearing behind the Lulou header.

I DO NOT WANT THIS.

REQUIRED BEHAVIOUR

On the PROFILE PAGE ONLY:

The entire Lulou header must be part of the normal page scroll.

When I scroll DOWN the Profile page:
- the header should move upward with the rest of the Profile content;
- the logo, “Lulou” wordmark and logout icon should naturally scroll off-screen;
- absolutely no Profile content should travel behind the header.

When I scroll back to the top:
- the Lulou header naturally returns with the page.

Think of the Profile page as ONE continuous vertically scrolling document.

IMPORTANT

Do NOT simply add extra top padding to hide the problem.

Find the actual cause.

Inspect for:
- position: fixed
- position: sticky
- sticky top-0
- fixed top-0
- absolute positioning
- separate scroll containers
- overflow-y containers
- z-index/header overlay behaviour
- AppLayout/shared-header behaviour

If AppLayout owns the header and the Profile page owns a separate scroll container, correct the structure so the Profile header participates in the SAME scroll flow as the Profile content.

SCOPE

This request is specifically about Profile.

Do NOT accidentally change:
- Discover
- Intent / Intention Wheel
- Likes
- Connections
- Chat
- bottom navigation
- authentication
- profile data
- profile editing

Do not redesign the header.

Keep exactly the same:
- logo
- Lulou text
- logout icon
- dimensions
- colours
- typography
- border
- spacing

Only fix its scrolling behaviour.

BOTTOM NAVIGATION

The bottom navigation SHOULD remain fixed/stationary at the bottom exactly as it currently does.

Only the TOP Lulou header should become part of the Profile page's scrolling content.

IPHONE / SAFE AREA

This must work properly in the installed iPhone app/PWA.

At the very top of the page:
- respect the iPhone safe area/status bar;
- do not create a white gap;
- do not place the header underneath the status bar.

Once scrolling begins:
- the header should simply travel upward and disappear naturally.

ACCEPTANCE TEST

On an iPhone-sized viewport:

1. Open Profile.
2. Start at the very top.
3. Confirm Lulou header looks exactly as it does now.
4. Scroll downward.
5. Logo + Lulou + logout icon must move upward with the Profile.
6. Profile name and content must NOT pass underneath the header.
7. Continue scrolling until the header is completely off-screen.
8. Bottom navigation must remain fixed.
9. Scroll back to the top and confirm the header returns naturally.
10. Confirm other pages have not had their header/scroll behaviour accidentally changed.

Before editing:
- inspect the existing AppLayout/Profile scroll structure;
- identify the exact cause;
- make the smallest correct fix.

Afterwards report:
- root cause;
- exact files changed;
- exact CSS/layout behaviour changed;
- build result;
- mobile verification result;
- commit hash;
- origin/main hash.

Do not claim real-iPhone verification unless it was actually performed.