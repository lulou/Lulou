URGENT: Fix Lulou password-reset email authentication, phishing warning and broken email body.

CURRENT PRODUCTION FAILURE

A real Lulou password-reset email was delivered to Gmail, but Gmail displays:

“This message might be dangerous”

The email body also appears blank and shows only “...”.

The sender details currently show:

From: lulou <nonreply@luloudating.com>

This is not acceptable for a production authentication email.

This is not a logo issue. Investigate sender authentication, SMTP configuration, domain alignment and the actual reset-password template.

DO NOT tell the user to press “Looks safe”.

1. VERIFY THE EXACT EMAIL PROVIDER

Determine whether Supabase Auth is currently sending through:

- Resend custom SMTP
- Supabase’s default SMTP
- another SMTP provider

Report the actual provider used for this delivered email.

Check the Supabase project’s current:

- SMTP enabled/disabled state
- SMTP host
- SMTP port
- SMTP username
- sender name
- sender email

Do not expose the SMTP password or API key.

The production project must use custom SMTP, not Supabase’s demonstration sender.

2. FIX THE FROM ADDRESS

Use one consistent production sender:

Lulou <noreply@luloudating.com>

Do not mix:

- nonreply@luloudating.com
- noreply@luloudating.com
- Supabase sender domains
- unverified From addresses

Verify that noreply@luloudating.com is permitted by the SMTP provider and belongs to the verified luloudating.com domain.

3. VERIFY SPF, DKIM AND DMARC

Inspect the live DNS records for luloudating.com.

Verify:

SPF:
- exactly one valid SPF TXT record
- includes the actual email provider
- no duplicate conflicting SPF records
- passes for the envelope/return-path sender

DKIM:
- the provider’s DKIM selector records exist
- the delivered email is DKIM-signed
- DKIM uses d=luloudating.com or an aligned authorised subdomain
- DKIM result is PASS

DMARC:
- _dmarc.luloudating.com exists
- start with a valid monitoring policy if enforcement is not ready
- From-domain alignment passes through SPF or DKIM
- rua reporting address is configured where appropriate

Do not blindly add new DNS records without checking for existing duplicates.

4. INSPECT THE DELIVERED MESSAGE HEADERS

Use Gmail “Show original” or the provider delivery log and report:

- SPF result
- DKIM result
- DMARC result
- From domain
- Return-Path domain
- DKIM signing domain
- Message-ID domain
- sending IP/provider
- whether alignment passes

Identify the exact reason Gmail classified the message as dangerous.

Do not guess.

5. CHECK RESEND OR PROVIDER LOGS

Find the delivered password-reset email in the email provider dashboard.

Report:

- delivery status
- recipient
- sender
- subject
- provider message ID
- bounced, complained, delivered or suppressed status
- whether the recipient is on a suppression list
- any authentication warnings
- any domain-verification errors

Remove the recipient from suppression only if the suppression is confirmed safe to clear.

6. FIX THE PASSWORD-RESET TEMPLATE

Open the active Supabase template:

Authentication → Emails → Reset password

Confirm that the template saved in Supabase is actually the latest:

supabase-email-templates/2-password-reset.html

The active HTML must contain:

- the approved dark-plum Lulou logo from the public Supabase Storage URL
- a clear “Reset your password” heading
- explanatory text
- one obvious reset-password CTA
- a visible fallback URL
- a security note
- footer links
- no JavaScript
- no forms
- no tracking pixels
- no malformed HTML
- no missing closing tags
- no unsupported external CSS
- no empty body
- no placeholder-only content

Use the correct Supabase recovery variable:

{{ .ConfirmationURL }}

Verify it appears in both:

- the CTA href
- the visible fallback-link section

Do not use an empty, undefined or incorrect variable.

7. EMAIL HTML SAFETY

Use conservative email HTML:

- table-based layout
- inline CSS for critical styles
- absolute HTTPS image URL
- explicit image width and height
- useful alt text
- no SVG embedded in the email
- no scripts
- no iframes
- no data URLs
- no CSS that hides the main content
- no display:none around essential copy
- no suspicious shortened links
- no links to unrelated domains

All visible links should use:

https://www.luloudating.com

or the legitimate Supabase verification URL generated by {{ .ConfirmationURL }}.

8. TURN OFF LINK TRACKING FOR AUTH EMAILS

If Resend/provider click tracking or open tracking is enabled for authentication emails, disable it.

Password-reset links must not be rewritten through an unrelated tracking domain.

Confirm the delivered CTA resolves to the legitimate Supabase verification endpoint and then redirects only to the approved Lulou callback.

9. PLAIN-TEXT FALLBACK

Ensure the reset email has a readable plain-text alternative containing:

- Lulou
- the password-reset purpose
- the complete reset URL
- expiration/security guidance

The message must not become blank if Gmail strips part of the HTML.

10. TEST WITH A FRESH RESET EMAIL

After fixing DNS, SMTP and the template:

1. Request a brand-new password reset.
2. Confirm it arrives from:
  Lulou <noreply@luloudating.com>
3. Confirm Gmail shows no red dangerous-message warning.
4. Confirm the full branded body renders.
5. Confirm the dark-plum logo loads.
6. Confirm the reset CTA is visible.
7. Confirm the fallback URL is visible.
8. Confirm the link opens the correct Lulou recovery screen.
9. Set a new password.
10. Confirm the new password works on phone and laptop.

Test in:

- Gmail mobile
- Gmail desktop
- Apple Mail if available

11. REPORT

Provide:

- exact root cause of Gmail’s warning
- actual SMTP provider
- SPF result
- DKIM result
- DMARC result
- From and Return-Path alignment
- whether tracking rewrote the reset link
- exact reason the body displayed only “...”
- active reset-template subject
- files changed
- DNS changes required
- Supabase dashboard changes required
- provider-dashboard changes required
- commit hash
- PASS or FAIL for the fresh Gmail test

Do not mark this complete until Gmail receives a fully rendered Lulou password-reset email with no dangerous-message warning.